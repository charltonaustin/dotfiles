eval "$(rbenv init -)"
eval "$(pyenv virtualenv-init -)"
eval "$(pyenv init -)"
if [[ "$(uname -a)" =~ .*Darwin.* ]]
	then
		echo "On Mac"
		eval $(dinghy env)
		bindkey "^X\\x7f" backward-kill-line
	else
		echo "Unknow system"
fi
